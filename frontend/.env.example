# Frontend environment variables for PathForge

# API base URL for backend requests
# Use the deployed backend URL in production, or keep localhost for local dev
VITE_API_BASE_URL=http://localhost:5000/api
